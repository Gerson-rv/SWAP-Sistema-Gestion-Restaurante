@model PRJ_Panaderia.Models.Categoria

@{
    ViewData["Title"] = "Editar Categoría";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow">
                <div class="card-header bg-warning">
                    <h4 class="mb-0"><i class="bi bi-pencil-square"></i> Editar Categoría</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="IdCategoria" />

                        <div class="mb-3">
                            <label asp-for="Nombre" class="form-label"></label>
                            <input asp-for="Nombre" class="form-control" id="txtNombre" placeholder="Ingrese el nombre de la categoría" />
                            <span asp-validation-for="Nombre" class="text-danger"></span>
                        </div>

                        <div class="mb-3 form-check form-switch">
                            <input asp-for="Activo" class="form-check-input" role="switch" id="chkActivo" />
                            <label asp-for="Activo" class="form-check-label" for="chkActivo"></label>
                        </div>

                        <div class="d-flex gap-2 mt-4">
                            <button type="submit" class="btn btn-warning">
                                <i class="bi bi-save"></i> Actualizar
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Cancelar
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var chk = document.getElementById('chkActivo');
            var txtNombre = document.getElementById('txtNombre');

            function toggleCampos() {
                var soloLectura = !chk.checked;
                txtNombre.readOnly = soloLectura;
                txtNombre.style.backgroundColor = soloLectura ? '#e9ecef' : '';
            }

            toggleCampos();
            chk.addEventListener('change', toggleCampos);
        });
    </script>
}
