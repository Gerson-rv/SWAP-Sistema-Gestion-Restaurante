@model PRJ_Panaderia.Models.Cliente

@{
    ViewData["Title"] = "Editar Cliente";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow">
                <div class="card-header bg-warning">
                    <h4 class="mb-0"><i class="bi bi-pencil-square"></i> Editar Cliente</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="IdCliente" />

                        <div class="mb-3">
                            <label asp-for="NombreCompleto" class="form-label"></label>
                            <input asp-for="NombreCompleto" class="form-control" id="txtNombre" placeholder="Ingrese el nombre completo" />
                            <span asp-validation-for="NombreCompleto" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Dni" class="form-label"></label>
                            <input asp-for="Dni" class="form-control" id="txtDni" maxlength="8" placeholder="Ingrese el DNI (8 dígitos)" />
                            <span asp-validation-for="Dni" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Telefono" class="form-label"></label>
                            <input asp-for="Telefono" class="form-control" id="txtTelefono" maxlength="9" placeholder="Ingrese el teléfono (9 dígitos)" />
                            <span asp-validation-for="Telefono" class="text-danger"></span>
                        </div>

                        <div class="mb-3 form-check form-switch">
                            <input asp-for="Activo" class="form-check-input" role="switch" id="chkActivo" />
                            <label asp-for="Activo" class="form-check-label" for="chkActivo"></label>
                        </div>

                        <div class="d-flex gap-2 mt-4">
                            <button type="submit" class="btn btn-warning">
                                <i class="bi bi-save"></i> Actualizar
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Cancelar
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var chk = document.getElementById('chkActivo');
            var txtNombre = document.getElementById('txtNombre');
            var txtDni = document.getElementById('txtDni');
            var txtTelefono = document.getElementById('txtTelefono');

            function toggleCampos() {
                var soloLectura = !chk.checked;
                txtNombre.readOnly = soloLectura;
                txtDni.readOnly = soloLectura;
                txtTelefono.readOnly = soloLectura;
                txtNombre.style.backgroundColor = soloLectura ? '#e9ecef' : '';
                txtDni.style.backgroundColor = soloLectura ? '#e9ecef' : '';
                txtTelefono.style.backgroundColor = soloLectura ? '#e9ecef' : '';
            }

            toggleCampos();
            chk.addEventListener('change', toggleCampos);
        });
    </script>
}
